= form_with(model: map, class: "contents") do |form|
  - if map.errors.any?
    #error_explanation.bg-red-50.text-red-500.px-3.py-2.font-medium.rounded-lg.mt-3

    h2
      = pluralize(map.errors.count, "error")
      |  prohibited this map from being saved:
    ul
      - map.errors.each do |error|
        li
          = error.full_message
  .inline
    = form.submit class: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer"
